#!/bin/bash
set -e
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
mkdir -p $DIR/../artifacts
helm3 dep update $DIR/../charts/*
cd $DIR/../artifacts
helm3 package $DIR/../charts/*
helm3 repo index . --url http://helm.steven-miller.me
